PORT=3000
PARQUET_PATH=path/to/data/data_full.parquet
CORS_ORIGIN=http://localhost:5173,http://localhost:3001
IMF_MAX_FIELDS=5

# V-Dem relationships explain endpoint
# Path to definitions JSON (index metadata). Defaults to src/definitions.json
RESPONSE_JSON_PATH=./src/definitions.json
# Path to correlations parquet. Defaults to data/correlations.parquet
CORRELATIONS_PARQUET_PATH=./data/correlations.parquet
# OpenAI model (used when execute=true). Default is gpt-4o
OPENAI_MODEL=gpt-4.1
# OpenAI API key (only required when execute=true)
OPENAI_API_KEY=